//! # Factorial and Fibonacci generator
//! Generate factorial and fibonacci sequences from a range

/// Everything is in module 'lib'
pub mod lib;

/// The only thing that main function do is calling `lib::main::main`
pub fn main() {
    lib::main::main()
}
